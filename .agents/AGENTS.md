# Project Rules

- Always install all python libraries and run all python commands inside the `berat_env` conda environment.
- Prefix commands with `conda run -n berat_env` or ensure the environment is activated before running `python` or `pip`.
